# mongod.conf - MongoDB Community Server configuration for Mbrain
# Adapted from github.com/JohnGUnderwood/mdb-community-search

net:
   port: 27017
   bindIpAll: true

replication:
   replSetName: rs0

setParameter:
   # mongot connection parameters (required for $vectorSearch, $search)
   searchIndexManagementHostAndPort: mbrain-mongot.mbrain-net:27028
   mongotHost: mbrain-mongot.mbrain-net:27028
   skipAuthenticationToSearchIndexManagementServer: false
   useGrpcForSearch: true

security:
   authorization: enabled
   keyFile: /auth/keyfile
