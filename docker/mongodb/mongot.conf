# mongot.conf - MongoDB Community Search configuration for Memongo
# Adapted from github.com/JohnGUnderwood/mdb-community-search

syncSource:
   replicaSet:
      hostAndPort: "memongo-mongod.memongo-net:27017"
      username: "mongotUser"
      passwordFile: "/auth/passwordFile"
      authSource: "admin"
      tls: false

storage:
   dataPath: "/data/mongot"

server:
   grpc:
      address: "memongo-mongot.memongo-net:27028"

metrics:
   enabled: true
   address: "memongo-mongot.memongo-net:9946"

healthCheck:
   address: "memongo-mongot.memongo-net:8080"

logging:
   verbosity: INFO

# The setup-generator writes a runtime config to /auth/mongot.generated.yml.
# When embedding keys are present it appends:
# embedding:
#    queryKeyFile: /auth/voyage-api-query-key
#    indexingKeyFile: /auth/voyage-api-indexing-key
#    providerEndpoint: https://ai.mongodb.com/v1/embeddings
#    isAutoEmbeddingViewWriter: true
